#include <algorithm>
#include <vector>
#include <math.h>
#include <sstream>
#include <iostream>

/**
    Coding Test
    Program1.cpp
    Purpose: Calculates the total of tokens on an 8×8 checkerboard (method a)

    @author Tinhinane Ait Hamouda
    @version 1.0 21/04/18 
*/

float method_a(std::vector<float>& checkerboard);
std::string to_decimal(const float& sum);
void display_checkerboard(const std::vector<float>& checkerboard);
